{
  "$schema": "https://json-schema.org/schema",
  "$id": "AddTypecheckSpec",
  "title": "Add typecheck-spec target to a project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add the typecheck-spec target to",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which project would you like to add the typecheck-spec target to?"
    },
    "all": {
      "type": "boolean",
      "description": "Add typecheck-spec target to all projects that have tsconfig.spec.json",
      "default": false
    },
    "force": {
      "type": "boolean",
      "description": "Overwrite existing typecheck-spec targets",
      "default": false
    }
  },
  "required": []
}
