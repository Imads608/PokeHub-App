# backend-pokehub-move-anim-catalog

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-pokehub-move-anim-catalog` to build the library.

## Running unit tests

Run `nx test backend-pokehub-move-anim-catalog` to execute the unit tests via [Jest](https://jestjs.io).
