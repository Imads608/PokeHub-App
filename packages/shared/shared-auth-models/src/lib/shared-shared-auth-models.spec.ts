import { sharedSharedAuthModels } from './shared-shared-auth-models';

describe('sharedSharedAuthModels', () => {
  it('should work', () => {
    expect(sharedSharedAuthModels()).toEqual('shared-shared-auth-models');
  });
});
