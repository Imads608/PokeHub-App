import { sharedSharedTeamModels } from './shared-shared-team-models';

describe('sharedSharedTeamModels', () => {
  it('should work', () => {
    expect(sharedSharedTeamModels()).toEqual('shared-shared-team-models');
  });
});
