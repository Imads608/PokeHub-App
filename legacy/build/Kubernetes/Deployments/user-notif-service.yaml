apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: user-notif-service
  name: user-notif-service
  namespace: pokehub-user
  annotations:
    'sidecar.jaegertracing.io/inject': 'true'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-notif-service
  template:
    metadata:
      labels:
        app: user-notif-service
    spec:
      containers:
        - image: registry.gitlab.com/imadsheriff97/pokehub-app/user-notif-service:dev
          name: user-notif-service
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 256Mi
            limits:
              memory: 500Mi
          env:
            - name: APPLICATION_NAME
              value: User Notification Service
            - name: APPLICATION_PORT
              value: '3000'
            - name: RABBITMQ_SERVICE
              value: 'host.docker.internal'
            - name: RABBITMQ_PORT
              value: '5672'
            - name: DB_HOST
              value: 'postgres.postgres.svc.cluster.local'
            - name: DB_PORT
              value: '5432'
            - name: DB_NAME
              value: 'users'
            - name: DB_USERNAME
              value: 'postgres'
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD

---
apiVersion: v1
kind: Service
metadata:
  name: user-notif-service
  namespace: pokehub-user
spec:
  type: ClusterIP
  selector:
    app: user-notif-service
  ports:
    - name: http
      targetPort: 3000
      port: 3000
