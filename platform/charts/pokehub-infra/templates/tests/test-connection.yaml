apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokehub-infra.fullname" . }}-test-connection"
  labels:
    {{- include "pokehub-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokehub-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
