import { TokenValidatorInterceptor } from './token-validator.interceptor';

describe('TokenValidatorInterceptor', () => {
  it('should be defined', () => {
    expect(new TokenValidatorInterceptor()).toBeDefined();
  });
});
