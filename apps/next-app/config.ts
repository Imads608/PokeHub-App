const appConfig: { apiGateway: string; chatService: string, frontend: string } = {
  apiGateway: 'http://localhost:3015',
  chatService: 'http://localhost:3002',
  frontend: 'http://localhost:4200'
};

export default appConfig;
