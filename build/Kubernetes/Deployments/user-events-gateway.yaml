apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: user-events-gateway
  name: user-events-gateway
  namespace: pokehub-user
  annotations:
    'sidecar.jaegertracing.io/inject': 'true'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-events-gateway
  template:
    metadata:
      labels:
        app: user-events-gateway
    spec:
      containers:
        - image: registry.gitlab.com/imadsheriff97/pokehub-app/user-events-gateway:dev
          name: user-events-gateway
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 256Mi
            limits:
              memory: 500Mi
          env:
            - name: APPLICATION_NAME
              value: User Events Gateway
            - name: APPLICATION_PORT
              value: '3000'
            - name: RABBITMQ_SERVICE
              value: 'host.docker.internal'
            - name: RABBITMQ_PORT
              value: '5672'

---
apiVersion: v1
kind: Service
metadata:
  name: chat-events-gateway
  namespace: pokehub-user
spec:
  type: ClusterIP
  selector:
    app: user-events-gateway
  ports:
    - name: http
      targetPort: 3000
      port: 3000
