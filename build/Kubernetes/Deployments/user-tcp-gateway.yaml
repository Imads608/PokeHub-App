apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: user-tcp-gateway
  name: user-tcp-gateway
  namespace: pokehub-user
  annotations:
    'sidecar.jaegertracing.io/inject': 'true'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-tcp-gateway
  template:
    metadata:
      labels:
        app: user-tcp-gateway
    spec:
      containers:
        - image: registry.gitlab.com/imadsheriff97/pokehub-app/user-tcp-gateway:dev
          name: user-tcp-gateway
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 256Mi
            limits:
              memory: 500Mi
          env:
            - name: APPLICATION_NAME
              value: User TCP Gateway
            - name: APPLICATION_PORT
              value: '3000'
            - name: USER_SERVICE_HOST
              value: 'user-service.pokehub-user.svc.cluster.local'
            - name: USER_SERVICE_PORT
              value: '3000'

---
apiVersion: v1
kind: Service
metadata:
  name: user-tcp-gateway
  namespace: pokehub-user
spec:
  type: ClusterIP
  selector:
    app: user-tcp-gateway
  ports:
    - name: tcp
      targetPort: 3000
      port: 3000
